import styled from 'styled-components';
import { StyledContentWrapper } from '../../styles/layout';

export const StyledAppContentWrapper = styled(StyledContentWrapper)`
  margin-top: 120px
  margin-bottom: 2rem;

`;

